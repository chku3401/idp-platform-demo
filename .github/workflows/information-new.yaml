name: information-new CI

on:
  push:
    branches: [ main ]
    paths: [ 'generated/information-new/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'generated/information-new/**' ]

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: generated/information-new
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Unit tests
        run: mvn -B test

      - name: Package
        run: mvn -B -DskipTests package

      - name: Build container image
        run: docker build -t information-new:${{ github.sha }} .

      - name: Scan container image
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: information-new:${{ github.sha }}
          severity: CRITICAL,HIGH
          exit-code: '0'
