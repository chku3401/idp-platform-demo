name: payment-api CI

on:
  push:
    branches: [ main ]
    paths: [ 'generated/payment-api/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'generated/payment-api/**' ]

permissions:
  contents: write
  packages: write

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: generated/payment-api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Unit tests
        run: mvn -B test

      - name: Package
        run: mvn -B -DskipTests package

      - name: Build container image
        run: docker build -t ghcr.io/chku3401/payment-api:${{ github.sha }} .

      - name: Scan container image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/chku3401/payment-api:${{ github.sha }}
          severity: CRITICAL,HIGH
          exit-code: '0'

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image
        if: github.event_name == 'push'
        run: docker push ghcr.io/chku3401/payment-api:${{ github.sha }}

      - name: Bump Helm values to the new image tag
        if: github.event_name == 'push'
        run: |
          sed -i "s/tag: .*/tag: ${{ github.sha }}/" helm/values.yaml
          if ! git diff --quiet helm/values.yaml; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add helm/values.yaml
            git commit -m "chore: bump payment-api image to ${{ github.sha }} [skip ci]"
            git push
          fi
